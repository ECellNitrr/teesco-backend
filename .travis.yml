language: python
    python:
      - "3.6"
    env:
      -DJANGO=3.0.5
    install:
      - pip install -r requirements.txt
    before_script:
      - python manage.py migrate
    script:
      - python manage.py test